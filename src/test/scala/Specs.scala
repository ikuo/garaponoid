import com.github.ikuo.garaponoid
import org.scalatest.matchers.ShouldMatchers
import org.scalatest.FunSpec

class Specs extends FunSpec with ShouldMatchers {
  describe("a spec") {
    it("should do something") {
    }
  }
}
